[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = ["slow: runs against the full sample workbook"]

[project]
name = "dock-scheduler"
version = "0.2.0"
requires-python = ">=3.12,<3.13"

[tool.vercel]
entrypoint = "app:app"
