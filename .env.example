# Vercel: connect a Neon Postgres database and use its pooled connection string.
# Configure this in Vercel's environment variables. Never commit real credentials.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Local usage can omit DATABASE_URL and use a persistent SQLite file instead.
# DOCK_DB=dock.db
